# GUI_mod 视觉约束

> 适用范围：`GUI_mod.py` 启动的 Fluent 2 风格界面，主要实现位于 `src/virtual_array/gui_mod.py`。

## 设计定位

`GUI_mod` 是 MIMO 阵列可视化工具的现代化桌面外观版本。视觉目标是保留 Windows 11 / Fluent 2 的系统亲和力，同时适配工程测试工具的高信息密度场景。

核心感受：

- 现代、轻盈、系统感强。
- 像 Windows 生态中的控制面板或工程工作台，而不是普通网页看板。
- 信息密度可以高，但边界、层级和操作优先级必须清楚。

## Fluent 2 视觉原则

界面应遵循以下方向：

- 背景使用浅蓝灰或浅色渐变感，不使用深色重背景。
- 主体内容使用浅色 Acrylic 卡片感：浅色底、细描边、轻高光。
- 主操作使用 Fluent 蓝，避免大面积高饱和色块。
- 圆角、描边和阴影保持克制，不做强拟物或夸张弹跳。
- 交互反馈应轻量：hover 变浅蓝、pressed 变深蓝或浅蓝，不大幅位移。

避免：

- 紫色/橙棕色等偏离 Fluent 2 的主色调。
- 过厚边框、立体凸起、老式 `ttk` 灰色控件质感。
- 过多独立小盒子导致工具栏碎片化。

## 品牌与图标

Logo 主元素是 `MAV` 三个字母，含义为 MIMO Array Visualizer。

当前资产：

- `src/virtual_array/assets/mimo_array_logo_48.png`：UI 左上角 logo。
- `src/virtual_array/assets/mimo_array_logo_256.png`：运行时窗口图标。
- `src/virtual_array/assets/mimo_array_logo.ico`：Windows 打包图标。
- `src/virtual_array/assets/mimo_array_logo.icns`：macOS 打包图标。

约束：

- 左上角 logo 与系统窗口/Dock/任务栏图标应使用同一套 MAV 视觉。
- Logo 应保持简洁，优先保证 48px 和 16px 下可识别。
- 不要把 logo 做成复杂射频图、设备图或长文字。
- 可保留极简阵列节点线作为辅助元素，但不能抢过 `MAV` 字母。

## 顶部 App Bar

顶部栏用于承载产品标识和全局菜单。

约束：

- 使用浅色 app bar，不使用黑色或深色菜单条。
- 左侧顺序为：MAV logo、应用名、分隔线、菜单组。
- 菜单保留 `文件`、`编辑`、`视图`、`帮助` 结构。
- 菜单按钮应是轻量文本按钮，不做大块填充。
- 分隔线要细、浅、低存在感。

## Header 信息区

Header 下方展示工作台说明和关键运行状态 chips。

左侧：

- 第一行是产品名。
- 第二行是简短说明：阵列布局、虚拟孔径、响应曲线与 DBF 扫描。

右侧 chips：

- `频率`
- `DBF字典`
- `方向图`
- `虚拟通道`
- `方位分辨率`
- `俯仰分辨率`
- `峰值裕量`

宽度约束：

- `方向图` chip 必须比普通数值 chip 更宽，避免文件名过早换行。
- `峰值裕量` 不应过宽，避免占用方向图和分辨率空间。
- 长文件名可截断显示，但不能撑高 header。
- 数值信息使用 Fluent 蓝突出，标签使用次级灰色。

## 主标签页

主工作区标签为：

- `Physical & Virtual`
- `1D DBF`
- `2D DBF`

约束：

- 不使用原生 `ttk.Notebook` 的立体标签外观。
- 标签应呈现为扁平 segmented tab。
- 选中态使用浅 Fluent 蓝底和深蓝文字。
- 未选中态使用透明或浅背景，只保留文字权重。
- 不使用灰色凸起边框、厚底边、错位边线。
- 标签宽度应稳定，三项视觉重量接近。

## 图表工作区

图表区域是核心工作面，不应被装饰元素抢占注意力。

约束：

- 图表卡片使用白色或接近白色背景。
- 外层可保留细描边，用于和页面背景分离。
- 网格线使用浅蓝灰，不能比数据点更突出。
- Tx/Rx、唯一点/重复点等颜色语义应稳定，不随主题随意变化。
- 图表标题和坐标轴必须保持清晰可读。

## 右侧 Overview

右侧 overview 是工程诊断摘要，不是装饰侧栏。

约束：

- 必须完整显示，不允许被主图或标签页压缩到裁切。
- 关键指标卡片保持一列纵向结构，便于扫描。
- `阵列概览` 与 `测角评估` 保持明确分组。
- 风险/不可用信息可以使用红色，但不要过度铺色。
- 右侧面板可跨过主标签页高度，保证垂直空间。

## 底部 Command Bar

底部是操作命令区，视觉上应是一条连续 command bar，而不是一排独立卡片。

包含：

- 频率输入。
- 竞争峰裕量输入。
- 自动排阵输入和 `应用阵列` 主按钮。
- DBF 显示模式。
- 方向图状态。
- 全局状态文本。

约束：

- 不给每个功能小模块画灰色外框线。
- 只保留内部输入框、radio、主按钮本身的边界。
- 各模块垂直基线应对齐。
- `应用阵列` 是底栏主操作，使用 Fluent 蓝按钮。
- 主按钮高度必须贴合底栏，不应明显高于输入框和 radio。
- 状态文本放在底部单独一行，保持低噪声。

## 颜色与状态

推荐色彩语义：

| 用途 | 颜色方向 |
| --- | --- |
| 主操作 / 关键数值 | Fluent 蓝 |
| 页面背景 | 浅蓝灰 |
| 卡片背景 | 白色或极浅蓝白 |
| 普通文字 | 深灰黑 |
| 次级文字 | 中性灰 |
| 成功 | 绿色，低面积使用 |
| 警告 | 琥珀色，低面积使用 |
| 风险 / 不可用 | 红色，仅用于文本或重点数值 |

不要用颜色同时表达太多含义。工程指标优先保持可读性。

## 跨平台图标要求

运行时：

- Tk 窗口图标通过 `iconphoto` 使用 PNG。
- Windows 下可优先尝试 `iconbitmap` 使用 `.ico`。

打包：

- Windows exe 使用 `mimo_array_logo.ico`。
- macOS app 使用 `mimo_array_logo.icns`。
- PyInstaller spec 需要把 logo assets 打进包内。

## 修改时的验收清单

每次调整 `GUI_mod` 视觉后，至少检查：

- 左上角 MAV logo 是否显示正常。
- macOS Dock / Windows 任务栏图标是否使用 MAV 图标。
- 主标签是否为扁平 segmented 风格，没有原生立体边框。
- 顶部 chips 没有不合理换行或宽度失衡。
- 右侧 overview 没有裁切。
- 底部 command bar 没有模块外框线，且高度一致。
- `应用阵列` 仍是唯一显著主按钮。
- 运行 `python3 -m py_compile src/virtual_array/gui_mod.py GUI_mod.py`。
- 运行项目轻量测试：`.venv/bin/python -m pytest tests/test_imports.py tests/test_geometry.py tests/test_analysis.py -q`。

