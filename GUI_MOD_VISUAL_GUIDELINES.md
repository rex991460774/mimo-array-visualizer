# MIMO Array Visualizer Carbon-led 原生 GUI 约束

> 适用范围：唯一正式 GUI 实现 `src/virtual_array/gui.py`。`GUI.py` 是正式启动器，`GUI_mod.py` 仅为兼容别名，两者必须打开完全相同的界面。

## 设计定位

MIMO Array Visualizer 使用原生 PySide6 控件和 Qt 绘图适配层。视觉结构与信息密度以 [Carbon Design System](https://carbondesignsystem.com/) 为主；交互层保留 [apple-design skill](https://github.com/emilkowalski/skills/tree/main/skills/apple-design) 的立即反馈、直接操控、可中断和克制动效原则。

这是 **Carbon-led、Apple-interaction 的产品级 Qt 工作台**，不是在 Qt 中嵌入 Carbon Web 组件，也不是仿制 macOS 外壳。保留平台原生窗口行为、Qt 控件语义和 Windows 用户习惯，能降低跨技术栈依赖并保持工程工具所需的信息密度。

可靠性约束：

- GUI 只维护 `virtual_array.gui` 一套实现，禁止在兼容启动器中复制布局或业务逻辑。
- 视觉和交互改造不得改变分析算法、文件格式、图表颜色语义或最终数值结果。
- 颜色、圆角、字体和状态必须来自 `src/virtual_array/native_theme.py` 的集中 token；不得在页面和弹窗中重复硬编码近似值。
- 使用 Qt 原生布局与尺寸策略适配 DPI 和窗口变化，不依赖固定像素坐标拼接界面。

## 窗口与响应式布局

- 默认尺寸为 **1366 × 768**，最小尺寸为 **1024 × 650 逻辑像素**，推荐工作尺寸不低于 **1100 × 650**，允许缩放和最大化。
- 恢复历史窗口位置和尺寸前，必须将几何范围限制在当前屏幕可用区域内。
- 主内容与 Overview 使用可拖动 `QSplitter`，默认比例约 78:22；Overview 目标宽度为 280–360 px。
- resize 触发的高成本图表刷新使用约 100 ms 防抖；阵元拖动刷新按约 16 ms 合并，避免事件风暴阻塞主线程。
- 不得因长文件名、翻译文本或 100%/125%/150% 缩放产生重叠、裁切或非预期滚动条。

## Carbon-led 主题与 token

界面使用 Carbon Gray 10 / White 的浅色中性层级，以 Blue 60 表达主操作、选择和焦点：

| 用途 | Token / 颜色 |
| --- | --- |
| 页面背景 | `#F4F4F4` |
| 卡片 / 表面 | `#FFFFFF` |
| 次级表面 | `#E8E8E8` |
| 选择底色 | `#D0E2FF` |
| 正文 | `#161616` |
| 次级文字 | `#525252` |
| 三级文字 | `#6F6F6F` |
| 分隔边框 | `#E0E0E0` |
| 交互边框 | `#8D8D8D` |
| 选择 / 焦点 / 图表强调 | `#0F62FE` |
| 主操作填充 | `#0F62FE` |
| 主操作 hover | `#0353E9` |
| 主操作 pressed | `#002D9C` |

- 使用 8 px 间距网格；普通控件与工作台表面采用 4 px 小圆角，避免卡片套卡片和胶囊化。
- 字体遵循平台并优先保证本地化字形完整：Windows 使用 Microsoft YaHei UI / Segoe UI，macOS 使用 SF Pro / PingFang，Linux 使用 Noto Sans CJK / Noto Sans，并保留后备链。
- 普通正文和关键按钮文字对比度至少 4.5:1；控件边界与相邻背景至少 3:1。
- 键盘焦点使用清晰的 2 px 系统蓝轮廓，不能只依赖轻微背景变化。
- hover 与 pressed 必须即时响应；不使用厚重阴影、拟物凸起、循环呼吸或装饰性弹跳。
- 红色只用于无效输入、加载失败、破坏性动作或明确风险；普通能力不足使用中性灰，边界风险使用琥珀色。

## 品牌、菜单与 Header

- 左上角和窗口/任务栏图标统一使用 MAV 资产：48 px PNG 用于界面，256 px PNG/ICO/ICNS 用于运行时和打包。
- 使用原生 `QMenuBar`/`QAction`，保留“文件、编辑、视图、帮助”结构及现有快捷键。
- 打开、保存、播放、停止等通用动作优先使用 Qt 标准图标，不增加新的图标依赖。
- Header 紧凑显示产品身份与三个全局状态：**频率、DBF 字典、通道幅相**；状态使用扁平单行标签和值，右对齐并支持省略。
- 虚拟通道、分辨率等阵列指标只在 Overview 显示，避免重复。
- 长文件名必须单行省略，并通过 tooltip 提供完整内容，不能撑高 Header。

## 标签页、表面与图表

中文、英文和日文页签都使用 Carbon 式线性标签页：未选中项与工作台表面融合，选中项使用浅蓝底和 3 px Blue 60 底边；键盘焦点必须清晰，不得使用原生立体标签或胶囊分段槽。

- **Physical & Virtual**：物理阵列与虚拟阵列并排。
- **1D DBF**：方位和俯仰响应谱并排。
- **2D DBF**：二维 DBF 热图及播放控制。

图表区约束：

- 卡片使用白色或近白背景与细描边；网格线使用低对比中性灰，不能压过数据。
- Tx/Rx、唯一点/重复点、真实角、峰值和风险的既有颜色语义保持稳定。
- 1T1R 或某方向无有效孔径时，使用中性空状态说明需要增加阵元或自动排阵，不绘制平直角谱或整片伪热图。
- 仅一个方向有效时，1D 页保留有效图表，另一侧显示能力提示；2D 页提示转到 1D 查看。
- 热图信息采用轻量徽标，不使用遮挡数据的大面积深色浮层。

## 直接操控与性能

物理阵元拖拽必须遵守以下完整生命周期：

1. `press`：记录指针与阵元中心的 grab offset，并捕获指针。
2. `move`：按该 offset 连续更新位置，只做边界裁切；不得每一帧吸附网格。
3. `move` 重绘：通过约 16 ms 的单次定时任务合并高频事件，已排队时不重复排队。
4. `release`：取消待处理重绘、执行一次网格吸附、生成虚拟阵列，并释放指针捕获。
5. Esc 取消、系统撤销鼠标捕获、切页或销毁窗口时也必须走同一清理路径：恢复按下前快照、释放捕获并取消定时任务，避免“粘住”或延迟回调访问无效控件。

这一顺序很重要：grab offset 消除阵元跳到指针中心的问题，指针捕获保证移出图表后仍能可靠结束拖拽，松手吸附消除逐格抖动，16 ms 合并刷新减少冗余计算并保持即时感。

1D 真实角引导线和 2D 十字线同样在按下时捕获、释放时归还指针。数据播放是用户主动启动的工程动画，不增加额外弹簧或惯性；停止、拖动和切页应立即打断播放。

## Overview、Command Bar 与状态栏

- Overview 是工程诊断摘要，按“阵列概览”和“测角评估”分组，并保持一列纵向扫描结构。
- Overview 展示通道数、虚拟通道、口径、分辨率、不模糊范围、最大误差、峰值裕量和截断原因。
- 底部操作区是一条连续 command bar，不给每个功能模块增加独立外框。
- 频率、峰值裕量、自动排阵、DBF 显示模式等控件应在同一垂直基线上。
- “应用阵列”是唯一显著的蓝色主按钮，其高度与相邻输入框一致。
- 普通运行状态移入原生 `QStatusBar`，保持低噪声，不占用 Header 或图表空间。

## 原生弹窗、键盘与可访问性

- DBF 字典、通道幅相、方向图确认和使用说明统一使用原生 `QDialog`。
- 数据预览使用 `QAbstractTableModel + QTableView`：少量列自动拉伸，多列使用横向滚动。
- 提示区使用带标题的中性、警告或风险 callout；文本自动换行，非阻断项明确标注“仅提示”。
- 按钮使用 `QDialogButtonBox` 的平台顺序；“应用/导入”为主按钮，“完成/取消”为次按钮。
- 使用说明使用 `QTextBrowser`；文件选择和普通消息使用 `QFileDialog`/`QMessageBox`。
- 所有控件必须有合理的 Tab 顺序、可见键盘焦点、accessible name 和本地化 tooltip。
- 颜色不能是唯一状态信号；危险、警告、选中和禁用状态同时使用文本、边框、图标或控件状态表达。
- 尊重系统字体缩放和高对比需求。当前产品不添加装饰性连续动画，因此“减少动态效果”用户不会丢失功能；DBF 播放仍由用户显式控制。

## 状态与兼容

- 继续读取 `state.json` v1；`window.active_tab` 和 `window.splitter_sizes` 为可选字段，缺失时使用默认值。
- 布局 JSON、DBF 字典、方向图、通道幅相格式不得因视觉改造改变。
- `main() -> None`、`VirtualArrayGui` 和 `virtual_array.gui` 已公开的纯计算辅助函数保持可导入。
- `GUI.py`、`GUI_mod.py`、console script 与 PyInstaller EXE 必须启动同一套三页界面。

## 修改验收清单

每次调整界面后至少验证：

- 1024×650、1100×650、1280×720、1366×768、1920×1080 逻辑窗口尺寸，以及 Windows 100%/125%/150% 缩放下无裁切或重叠。
- 中文、英文、日文页签、空状态、tooltip 和 accessible name 完整。
- 首启 1T1R 不显示红色错误、平直伪角谱或整片伪热图。
- 阵元按下时不跳位，移出图表后仍能正常释放，拖动过程连续，松手后才吸附网格。
- 1D 引导线和 2D 十字线的指针捕获、暂停播放与释放行为一致。
- Overview 可拖动且保持可读；应用阵列仍是唯一显著主操作。
- MAV 图标在窗口、任务栏和 PyInstaller 包中正常。
- 四个自定义弹窗、菜单快捷键、Tab 顺序、hover/pressed/focus/disabled 状态可用。
- 长文件名单行省略且 tooltip 完整。
- 运行静态检查：`.venv\Scripts\python.exe -m compileall -q GUI.py GUI_mod.py src\virtual_array`。
- 运行测试：`.venv\Scripts\python.exe -m pytest tests -q`。

重复截图验收必须通过 `scripts/capture_ui_review.py` 自动完成，并使用隔离的 `APPDATA` 生成三页和全部弹窗的编号截图，避免依赖个人历史状态。
