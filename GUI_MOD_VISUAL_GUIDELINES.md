# MIMO Array Visualizer 视觉约束

> 适用范围：唯一正式 GUI 实现 `src/virtual_array/gui.py`。`GUI.py` 是正式启动器，`GUI_mod.py` 仅为兼容别名，两者必须打开完全相同的界面。

## 设计定位

MIMO Array Visualizer 使用原生 PySide6 控件和 pyqtgraph 绘图，采用 Fluent 2 浅色工程工作台风格。界面应现代、轻盈、具有 Windows 系统亲和力，同时保证高信息密度场景下的层级与可读性。

可靠性相关约束：

- GUI 只维护 `virtual_array.gui` 一套实现，禁止在兼容启动器中复制布局或业务逻辑。
- 保留分析算法、文件格式和图表颜色语义；视觉改造不得改变数值结果。
- 使用 Qt 原生布局与尺寸策略适配 DPI 和窗口变化，不依赖固定像素坐标拼接界面。

## 窗口与响应式布局

- 默认尺寸为 **1366 × 768**，最小尺寸为 **1100 × 650**，允许缩放和最大化。
- 恢复历史窗口位置和尺寸前，必须将几何范围限制在当前屏幕的可用区域内。
- 主内容与 Overview 使用可拖动 `QSplitter`，默认比例约 78:22；Overview 目标宽度为 280–360 px。
- resize 触发的高成本图表刷新使用约 100 ms 防抖，拖动窗口时不得持续阻塞界面。
- 不得因长文件名、翻译文本或 100%/125%/150% 缩放产生重叠、裁切或非预期滚动条。

## Fluent 2 主题

统一从一个 token/QSS 主题取值，禁止在各弹窗和页面重复维护近似颜色：

| 用途 | Token / 颜色 |
| --- | --- |
| 页面背景 | `#F3F8FD` |
| 卡片/表面 | `#FFFFFF` |
| 正文 | `#1F2937` |
| 次级文字 | `#64748B` |
| 主操作 / 焦点 | `#0078D4` |
| 交互边框 | `#8A94A6` |

- 使用 8 px 间距网格、6 px 控件圆角、10 pt 正文和不低于 9 pt 的辅助文字。
- 普通正文和关键按钮文字对比度至少 4.5:1；控件边界与相邻背景至少 3:1。
- 键盘焦点使用清晰的 2 px Fluent 蓝轮廓，不能只依赖颜色极轻的背景变化。
- hover 使用浅蓝反馈，pressed 使用更深蓝或浅蓝；不使用明显位移、厚重阴影或拟物凸起。
- 页面保持浅蓝灰背景和白色表面，不使用深色顶栏或大面积高饱和色块。

## 品牌、菜单与 Header

- 左上角和窗口/任务栏图标统一使用 MAV 资产：48 px PNG 用于界面，256 px PNG/ICO/ICNS 用于运行时和打包。
- 使用原生 `QMenuBar`/`QAction`，保留“文件、编辑、视图、帮助”结构及现有快捷键。
- 打开、保存、播放、停止等通用动作优先使用 Qt 标准图标，不增加新的图标依赖。
- Header 只显示三个全局状态：**频率、DBF 字典、通道幅相**。
- 虚拟通道、分辨率等阵列指标只在 Overview 显示，避免重复。
- 长文件名必须单行省略，并通过 tooltip 提供完整内容，不能撑高 Header。

## 三页工作区

主标签页在中文、英文和日文下均需本地化，并使用扁平分段标签样式：

- **Physical & Virtual**：物理阵列与虚拟阵列并排。
- **1D DBF**：方位和俯仰响应谱并排。
- **2D DBF**：二维 DBF 热图及播放控制。

选中页使用浅 Fluent 蓝底和深蓝文字，未选中页使用透明或浅色背景。不得出现原生立体标签、厚底边或错位边线。

图表区约束：

- 卡片使用白色或近白背景与细描边；网格线为浅蓝灰，不能压过数据。
- Tx/Rx、唯一点/重复点、真实角、峰值和风险的既有颜色语义保持稳定。
- 1T1R 或某方向无有效孔径时，使用中性空状态说明需要增加阵元或自动排阵，不绘制平直角谱或整片伪热图。
- 仅一个方向有效时，1D 页保留有效图表，另一侧显示能力提示；2D 页提示转到 1D 查看。
- 红色只表示无效输入、加载失败或明确风险；能力不足用中性灰，边界风险用琥珀色。
- 热图信息采用轻量徽标，不使用遮挡数据的大面积深色或青色浮层。

## Overview、Command Bar 与状态栏

- Overview 是工程诊断摘要，按“阵列概览”和“测角评估”分组，并保持一列纵向扫描结构。
- Overview 展示通道数、虚拟通道、口径、分辨率、不模糊范围、最大误差、峰值裕量和截断原因。
- 底部操作区是一条连续 command bar，不给每个功能模块增加独立外框。
- 频率、峰值裕量、自动排阵、DBF 显示模式等控件应在同一垂直基线上。
- “应用阵列”是唯一显著的蓝色主按钮，其高度与相邻输入框一致。
- 普通运行状态移入原生 `QStatusBar`，保持低噪声，不占用 Header 或图表空间。

## 原生弹窗与表格

- DBF 字典、通道幅相、方向图确认和使用说明统一使用原生 `QDialog`。
- 数据预览使用 `QAbstractTableModel + QTableView`：少量列自动拉伸，多列使用横向滚动。
- 提示区使用带标题的中性、警告或风险 callout；文本自动换行，非阻断项明确标注“仅提示”。
- 按钮使用 `QDialogButtonBox` 的平台顺序；“应用/导入”为主按钮，“完成/取消”为次按钮。
- 使用说明使用 `QTextBrowser`；文件选择和普通消息使用 `QFileDialog`/`QMessageBox`。
- 所有控件应有合理的 Tab 顺序、键盘焦点、accessible name 和本地化 tooltip。

## 状态与兼容

- 继续读取 `state.json` v1；`window.active_tab` 和 `window.splitter_sizes` 为可选字段，缺失时使用默认值。
- 布局 JSON、DBF 字典、方向图、通道幅相格式不得因视觉改造改变。
- `main() -> None`、`VirtualArrayGui` 和 `virtual_array.gui` 已公开的纯计算辅助函数保持可导入。
- `GUI.py`、`GUI_mod.py`、console script 与 PyInstaller EXE 必须启动同一套三页界面。

## 修改验收清单

每次调整界面后至少验证：

- 1100×650、1366×768、1920×1080 和 Windows 100%/125%/150% 缩放下无裁切或重叠。
- 中文、英文、日文页签、空状态、tooltip 和 accessible name 完整。
- 首启 1T1R 不显示红色错误、平直伪角谱或整片伪热图。
- Overview 可拖动且保持可读；应用阵列仍是唯一显著主操作。
- MAV 图标在窗口、任务栏和 PyInstaller 包中正常。
- 四个自定义弹窗、菜单快捷键、Tab 顺序、hover/pressed/focus/disabled 状态可用。
- 长文件名单行省略且 tooltip 完整。
- 运行静态检查：`.venv\Scripts\python.exe -m compileall -q GUI.py GUI_mod.py src\virtual_array`。
- 运行测试：`.venv\Scripts\python.exe -m pytest tests -q`。

重复截图验收应通过自动化脚本完成，并使用隔离的 `APPDATA` 生成三页和全部弹窗的编号截图，避免依赖个人历史状态。
